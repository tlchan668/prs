import { HttpClient } from '@angular/common/http'

//build model in vs
export class Requestlines{
    id: number = 0;
    requestId: number;
    productId: number;
    quantity: number;

  constructor(){}

}